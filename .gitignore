# ==========================
# Python 基础忽略规则
# ==========================
# 编译产生的字节码，无需上传
__pycache__/
*.py[cod]
*$py.class

# ==========================
# IDE / 编辑器配置
# ==========================
# 如果你或别人用 PyCharm、VSCode，这些个人配置不需要同步
.idea/
.vscode/
*.swp

# ==========================
# 虚拟环境
# ==========================
venv/
env/

# ==========================
# 项目生成的数据
# ==========================
# 忽略具体的优化结果文件 (JSON)，因为这些是你本地跑出来的
results/*.json

# 忽略临时产生的图片 (如果有)
images/*.png
# 但是！如果你想保留某些演示图片（例如 demo.png），可以在下面加一行：
# !images/demo.png

# ==========================
# 操作系统生成的文件
# ==========================
.DS_Store
Thumbs.db

# ==========================
# 保护规则 (保留文件夹结构)
# ==========================
# 下面这些规则的意思是：忽略文件夹里的所有内容，但保留 .gitkeep 文件
# 这样 Git 就会上传一个空文件夹，别人下载代码时就不会报错说找不到文件夹

# 1. 结果文件夹
!results/.gitkeep

# 2. 如果你想保留 tracks 文件夹但暂时不上传任何赛道（或者只保留 .gitkeep）
# 目前建议还是上传 csv 文件，所以下面这两行我注释掉了，默认上传所有赛道
# tracks/*
# !tracks/*.csv

# 3. 车辆文件夹同理，默认上传 json
# cars/*
# !cars/*.json